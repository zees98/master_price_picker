name: master_price_picker
description: A new Flutter project.
publish_to: none
version: 1.0.0+1
environment:
  sdk: ">=2.3.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get_it: ^4.0.4
  logger: ^0.9.2
  stacked: ^1.7.6
  stacked_services: ^0.5.4+2
  responsive_builder: ^0.2.0+2
  equatable: ^1.2.4
  web_scraper: 
  http:
  url_launcher:
  flutter_barcode_scanner: ^2.0.0
  animated_splash_screen:
  google_fonts:
  flutter_screenutil: ^4.0.3+2
    #firebase
  firebase_core:
  firebase_auth: 
  cloud_firestore:
  # excel:
  downloads_path_provider: ^0.1.0
  permission_handler: ^5.1.0+2
  syncfusion_flutter_xlsio: ^19.3.46-beta
  open_file:
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
    uses-material-design: true
    assets:
        - assets/
